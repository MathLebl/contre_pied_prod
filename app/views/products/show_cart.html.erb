<% unless @cart.empty? %>
  <% @cart.each do |item| %>
    <h3><%= item["name"] %></h3>
    <%= image_tag item["photo_url"] %>
    <%= link_to "[X]", remove_from_cart_product_path(@products.find(item["id"])) %>
  <% end %>
<% else %>
  <p>Panier vide</p>
<% end %>
