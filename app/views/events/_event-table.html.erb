  <tbody>
    <% @events.each do |event|%>
    <tr>
      <td class="table-date"><%= l event.date %></td>
      <td class="table-name"><%= event.artist.name %></td>
      <td class="table-location"><%= event.location %></td>
      <% if event.ticket != nil %>
        <td class="table-ticket"><a href="<%= event.ticket %>"><i class="fas fa-ticket-alt"></i></a></td>
      <% else %>
        <td></td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
